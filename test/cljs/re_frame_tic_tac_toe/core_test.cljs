(ns re-frame-tic-tac-toe.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [re-frame-tic-tac-toe.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
